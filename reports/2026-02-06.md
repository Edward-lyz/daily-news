今日的 AI Infra 的新闻如下。

## 摘要
### 本周关键更新概览
- **FlashMLA**: 引入 `CUDAGuard` 与动态设备 ID 分配，提升 CUDA 资源管理安全性，代码改动行数为 +12 -2。\
- **CUTLASS**: 社区报告 GEMM+SiLU 组合在特定 GPU 上显著慢于单独 GEMM，提供了性能对比截图，提示后续需要优化 epilogue 实现。\
- **FlashInfer**: 完成两大功能升级：① 将 `grouped_gemm_nt_masked` 从 `cute_dsl` 移至 `gemm`，实现向后兼容并统一 API；② 为 SM120/SM121 添加 FP4 GEMM 新 tile 配置及可选 Stream‑K 调度器，显著提升大矩阵乘法吞吐。\
- **SGLang**: 大规模 CI 与文档改动，包括合并 Stage‑C 大规模 GPU 测试套件、为 NIXL 连接器加入混合模型 PD、扩展 Diffusion 文档、迁移 GPTQ‑Marlin GEMM 到 JIT、以及多项 bug 修复（如 HF 下载竞争、torch.compile 图中断等）。\
- **vLLM**: 多项底层与功能增强：① ROCm 平台检测中防止提前 CUDA 初始化；② 修复路由器类型判定与 `logprobs=0` 返回空列表问题；③ 支持 SM120/SM121 的 FP4 GEMM tile 与 Stream‑K；④ 引入 XPU wna16 kernel、CPU NEON BFMMLA BF16 加速、MiniCPM‑o 4.5、Voyage‑4‑nano 等新模型；⑤ 完善文档、CI、pre‑commit 检查以及 KV Connector 方法覆盖。\
- **Flash‑Attention**: 讨论在 `flash‑attention‑4` 中将 `e2e_res` 设为调优变量的可行性，提出直接调优该参数的建议。

## 具体内容分析
### deepseek-ai/DeepGEMM
昨日无更新。
### deepseek-ai/FlashMLA
## deepseek-ai/FlashMLA

**2026-02-06** - Zeyu WANG 添加了 CUDAGuard 和动态设备 ID 分配功能 ([#160](https://github.com/deepseek-ai/FlashMLA/pull/160))

修改文件:
- `csrc/sm100/prefill/dense/fmha_cutlass_bwd_sm100.cuh`: 添加 CUDAGuard 头文件并实现动态设备 ID 分配
- `csrc/sm100/prefill/dense/fmha_cutlass_fwd_sm100.cuh`: 添加 CUDAGuard 头文件并实现动态设备 ID 分配

变更统计: +12 -2
### NVIDIA/cutlass
## 提交

本周 NVIDIA/cutlass 仓库无代码提交。

## Issues

### [QST] Why does gemm + epilogue(silu) take so much time?

有用户报告在使用 CUTLASS 进行 GEMM + SiLU epilogue 操作时，性能显著下降。测试结果显示，GEMM + SiLU 的延迟远高于单独的 GEMM 操作，而 GEMM + Bias 则没有明显增加延迟。

该问题在 thor-u GPU 上复现，环境配置为 CUTLASS v4.3.5、CUDA Toolkit 12.8 和 Linux OS。用户附上了详细的性能对比截图。

Issue 链接：[https://github.com/NVIDIA/cutlass/issues/3007](https://github.com/NVIDIA/cutlass/issues/3007)
### flashinfer-ai/flashinfer
## 本次提交概览  

| PR | 简要说明 | 关键文件/模块 | 代码片段 |
|---|---|---|---|
| **#2503** –  refactor: Port upstream CUTLASS fixes and refactor grouped_gemm_nt_masked GEMM module location | 将 CUTLASS 上游修复迁移至本仓库，并把 `grouped_gemm_nt_masked` 从 `flashinfer.cute_dsl` 移动到 `flashinfer.gemm`，同时加入向后兼容的废弃提示。 | `flashinfer/cute_dsl/__init__.py`、`flashinfer/gemm/__init__.py`、`flashinfer/gemm/kernels/__init__.py`、`flashinfer/gemm/kernels/grouped_gemm_masked_blackwell.py`、`tests/gemm/test_cute_dsl_blockscaled_gemm.py` | ```python\n# flashinfer/gemm/__init__.py\ntry:\n    from .kernels.grouped_gemm_masked_blackwell import (\n        grouped_gemm_nt_masked,\n        Sm100BlockScaledPersistentDenseGemmKernel,\n        create_scale_factor_tensor,\n    )\n    _cute_dsl_kernels = [\"grouped_gemm_nt_masked\", \"Sm100BlockScaledPersistentDenseGemmKernel\", \"create_scale_factor_tensor\"]\nexcept ImportError:\n    pass\n``` |
| **#2460** –  perf: add fp4 GEMM tile configs and streamK scheduler for SM120 | 为 SM120/SM121 添加了 `(128,128,256)`、`(256,128,128)` 等新 tile 配置，并引入可选的 Stream‑K 调度器。 | `flashinfer/jit/gemm/core.py`、`include/flashinfer/gemm/cutlass_gemm_configs.h`、`include/flashinfer/gemm/fp4_gemm_cutlass_template_sm120.h`、`include/flashinfer/gemm/fp4_gemm_template_sm120.h` | ```c++\n// cutlass_gemm_configs.h\nstruct CutlassGemmConfig {\n    ...\n    bool use_stream_k = false;  // SM120: false = DP scheduler, true = StreamK scheduler\n};\n\n// core.py (JIT)\ncta_m_n_k_list = [\n    (128, 128, 128),\n    (128, 128, 256),\n    (256, 128, 128),\n]\n``` |

### 代码迁移要点  

- **废弃路径**：`flashinfer.cute_dsl` 中的 GEMM 符号现在仅作向后兼容，文档已标记 `.. deprecated::`。  
- **新公共 API**：所有用户应改为 `from flashinfer.gemm import grouped_gemm_nt_masked, create_scale_factor_tensor, Sm100BlockScaledPersistentDenseGemmKernel`。  
- **内部组织**：新增 `flashinfer/gemm/kernels` 包，用于存放 CuTe‑DSL 实现的内部 kernel，外部仅通过 `flashinfer.gemm` 暴露。  

### 性能改进概览  

- **SM120/SM121 新 tile**：在 FP4 GEMM 中加入更大 K 维度的 tile，提升大矩阵乘法吞吐。  
- **Stream‑K 调度**：通过 `use_stream_k` 标志可在 SM120 上启用 Stream‑K 调度器，针对深度学习工作负载提供更好的并行度。  

## 本期 Issue 汇总  

| Issue | 简要描述 |
|---|---|
| [#2514](https://github.com/flashinfer-ai/flashinfer/issues/2514) – **test_groupwise_scaled_gemm_mxfp4.py 在 B300 上偶发 NaN** | 在 SM103（B300）上运行 `test_groupwise_scaled_gemm_mxfp4.py` 时出现不确定性 NaN，需排查数值稳定性。 |
| [#2513](https://github.com/flashinfer-ai/flashinfer/issues/2513) – **为 Mamba Selective State Update 添加基准** | 新增的 Mamba 选择性状态更新 kernel 缺少微基准，需要补充 benchmark 脚本。 |
| [#2511](https://github.com/flashinfer-ai/flashinfer/issues/2511) – **TRTLLM‑GEN 后端 CUDA Graph 结果不一致** | 在 B200 上使用 `BatchDecodeWithPagedKVCacheWrapper` 与 TRTLLM‑GEN 时，开启 CUDA Graph 会导致结果偏差。 |
| [#2507](https://github.com/flashinfer-ai/flashinfer/issues/2507) – **trtllm_fp8_per_tensor_scale_moe 报 AttributeError** | `trtllm_fp8_per_tensor_scale_moe` 在 0.6.3 版本中因 `activation_type` 处理不当抛出 `'int' object has no attribute 'value'`。 |
| [#2506](https://github.com/flashinfer-ai/flashinfer/issues/2506) – **Jetson Orin NX 模块注册失败** | 在 Jetson Orin NX（CUDA 12.6）上执行 `flashinfer show-config` 时提示 `No module named 'nvidia'`，需要检查 Jetson 环境的依赖路径。 |

---  

**总体评估**：本次 PR 主要完成了 API 重构、向后兼容与性能升级三大任务。代码迁移提升了库的可维护性，新增的 SM120 tile 与 Stream‑K 调度为高端 GPU（如 H100、H200）提供了显著加速潜力。与此同时，若干已知 Bug（尤其是 B300 上的数值不稳定）仍待进一步调试。后续建议优先跟进 Issue #2514 与 #2507，以确保新功能在所有受支持硬件上保持正确性。
### Dao-AILab/flash-attention
**2026-02-06**

**Dao-AILab/flash-attention**

**Issue:**
- [Why not choose e2e_res as our tune variable](https://github.com/Dao-AILab/flash-attention/issues/2237) - cyk2018 提出关于在 flash-attention-4 中调整 e2e_res 和 e2e_freq 参数的问题，讨论了当前选择 e2e_freq 作为调优变量的合理性，建议考虑直接调整 e2e_res 参数。
### sgl-project/sglang
提交  
- **bedade1** – Merge stage‑c‑test‑large‑4‑gpu suites into partitioned suites (#18325)  
  - 作者：Alison Shao，2026‑02‑06 23:32:33 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/bedade1ef050eaae4cd2232e229b1d267efb66fd>  
  - 关键改动：  
    - `.github/workflows/pr-test.yml` 中删除了 `stage-c-test-large-4-gpu` 相关作业（87 行删除）。  
    - `scripts/ci/utils/slash_command_handler.py` 移除对 `stage-c-test-large-4-gpu` 与 `stage-c-test-large-4-gpu-b200` 的支持。  
    - `test/README.md` 将 Stage C 划分为 4‑GPU 与 8‑GPU 两套新套件，删除旧套件名称。  
    - 多个测试文件（`test_local_attn.py`、`test_dp_attention_large.py`、`test_cutedsl_moe.py`、`test_return_routed_experts.py`、`test_eagle_dp_attention.py`、`test_eagle_infer_beta_dp_attention.py`）的 `register_cuda_ci` 调用从 `stage-c-test-large-4-gpu` 替换为对应的 4‑GPU 或 4‑GPU‑b200 套件。  
    - `test/run_suite.py` 移除旧套件名称，加入新套件 `stage-c-test-4-gpu-h100`、`stage-c-test-4-gpu-b200`、`stage-c-test-4-gpu-gb200`。  
  - **说明**：`patch_truncated` 为 true，完整 diff 未展示，以上为关键摘要。

- **f1ff697** – add hybrid model PD to NIXL connector (#16229)  
  - 作者：Neal Vaidya，2026‑02‑06 23:05:36 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/f1ff697494c59bd0a4d298d5a1411f9eed563982>  
  - 关键改动：`python/sglang/srt/disaggregation/nixl/conn.py`  
    - 为 `TransferInfo` 增加 `dst_state_indices: List[int]` 字段。  
    - `from_zmq` 解析第 8 条消息为 `dst_state_indices`（若存在）。  
    - `KVArgsRegisterInfo` 新增 `dst_state_data_ptrs: list[int]`。  
  - **说明**：完整 diff 已提供（140 行改动）。

- **c850a8a** – [Docs] Add Falcon H1, Hunyuan‑Large, Qwen3‑Omni support and update Diffusion usage (#17888)  
  - 作者：Rishit Shivam，2026‑02‑06 21:17:51 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/c850a8a41af2d2f973a7cb033eef9ba9cacc0ca3>  
  - 关键改动：新增文档 `docs/basic_usage/diffusion.md`（介绍图像/视频扩散模型）和 `docs/basic_usage/diffusion_llms.md`（介绍扩散语言模型）。  
  - 其他文档路径更新：`openai_api_embeddings.ipynb`、`openai_api_vision.ipynb` 中的模型列表链接指向新的目录；`docs/index.rst` 添加 diffusion 文档入口；`docs/index.rst` 中的 “Supported Models” 章节层级调整。  
  - **说明**：部分文件被重命名或移动，`patch_truncated` 为 true，具体改动请参考提交。

- **f2e0048** – Add CI permission for Shunkangz, dongjiyingdjy, samuellees (#18377)  
  - 作者：Baizhou Zhang，2026‑02‑06 17:19:02 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/f2e0048d0688e929803f109bb633b327a9fb3d2c>  
  - 关键改动：`.github/CI_PERMISSIONS.json` 新增 3 位用户的 CI 权限（21 行新增）。  

- **e13b727** – [diffusion] CI: update perf baseline (#17512)  
  - 作者：Prozac614，2026‑02‑06 16:28:44 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/e13b727e928354e53b1e5ba3f613d8bb447a5594>  
  - 关键改动：`python/sglang/multimodal_gen/test/server/perf_baselines.json` 更新基准数据（+398 行，‑331 行）。  

- **c6aa186** – Add Nemotron 3 Nano tests (#18119)  
  - 作者：shaharmor98，2026‑02‑06 15:55:42 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/c6aa1863be84c77bf423ee811e03e9129401c539>  
  - 关键改动：  
    - `python/pyproject.toml` 新增依赖行（+1）。  
    - 新增 `python/sglang/test/kits/lm_eval_kit.py`（109 行）用于 LM‑Eval 测试。  
    - 新增两份 Nemotron‑3‑Nano 配置 YAML（`test/lm_eval_configs/...`），以及对应的注册测试 `test/registered/models/test_nvidia_nemotron_3_nano.py`（41 行）。  

- **79d409f** – [diffusion] fix: offload text encoder model in image encoding stage (#18317)  
  - 作者：xiaoye，2026‑02‑06 14:55:56 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/79d409f21019686d2ac462ad6287c2cc92751120>  
  - 关键改动：`python/sglang/multimodal_gen/runtime/pipelines_core/stages/image_encoding.py` 新增 1 行代码，实现文本编码器的 offload。  

- **3d68bd9** – add hicache jit test (#17847)  
  - 作者：Xuchun Shang，2026‑02‑06 08:54:33 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/3d68bd9d9b6ed994a2de1f7d9d2bd2956f8f1ac9>  
  - 关键改动：  
    - 新增 `python/sglang/jit_kernel/benchmark/bench_hicache.py`（406 行）用于 HiCache 基准。  
    - 多个 JIT kernel 基准文件（`bench_per_tensor_quant_fp8.py`、`bench_qknorm.py`、`bench_rmsnorm.py`、`bench_store_cache.py`、`utils.py`）分别做了细微增删（总计 +523 / ‑74 行）。  

- **f798ab9** – [diffusion] fix: fix torch.compile graph break caused by torch._dynamo.disable (#18336)  
  - 作者：郑一帆，2026‑02‑06 06:48:09 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/f798ab9775ba16b3b64e65f5c33a222ad5a64829>  
  - 关键改动：  
    - `python/sglang/jit_kernel/diffusion/cutedsl/scale_residual_norm_scale_shift.py` 调整实现（+32 / ‑20 行）。  
    - `python/sglang/jit_kernel/tests/test_fused_norm_scale_shift.py` 删除冗余断言（‑3 行）。  
    - `python/sglang/multimodal_gen/runtime/layers/layernorm.py` 小幅优化（+5 行）。  

- **d0c39bc** – Fix cross‑container HF download race condition in CI (#18328)  
  - 作者：Alison Shao，2026‑02‑06 05:01:41 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/d0c39bc2193c3e28eee94018340d32029541f0a3>  
  - 关键改动：`python/sglang/srt/model_loader/ci_weight_validation.py` 增加文件锁与重试逻辑（+99 行，‑13 行），防止多容器并发下载同一模型导致的冲突。  

- **92b8bd6** – fix npu best practice (#18330)  
  - 作者：amote-i，2026‑02‑06 02:14:46 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/92b8bd68333ef9bca081201cca474755e5e42a98>  
  - 关键改动：`docs/platforms/ascend_npu_best_practice.md` 中的 8 行删除与 7 行新增，更新了 NPU 使用建议。  

- **aa390d2** – [Kernel] Migrate GPTQ‑Marlin GEMM kernel to JIT (#18067)  
  - 作者：Linyu Wu，2026‑02‑06 00:31:42 UTC  
  - PR 链接：<https://github.com/sgl-project/sglang/commit/aa390d276279360899047567fe6892e28abed7cc>  
  - 关键改动：大量新增 JIT kernel 代码（共计约 4 k 行），包括：  
    - `bench_gptq_marlin.py`（118 行）基准脚本。  
    - `csrc/gemm/marlin/` 目录下的实现文件（`dequant.h`、`gptq_marlin.cuh`、`kernel.h`、`marlin.cuh`、`marlin_dtypes.cuh`、`marlin_template.h`）共计约 2.5 k 行。  
    - Python 包装层 `gptq_marlin.py`（115 行）与测试 `test_gptq_marlin.py`（125 行）。  
    - 相关量化工具 `marlin_utils.py`、`marlin_utils_fp8.py` 小幅改动（±2 行）。  

---

Issues  
- **[Feature] Return token ids under /chat/completion endpoint (Prompt + Response)**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18378>  
  - 简述：用户希望在 `/chat/completion` 接口返回生成 token 的 ID 与 prompt token 元信息，以保证 RL 训练的 token 一致性。  

- **Empty responses when streaming LLM outputs on large models (KeyError: prompt_tokens)**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18371>  
  - 简述：在 128k/332k 上下文长度下进行流式输出时，服务器抛出 `KeyError: 'prompt_tokens'`，导致客户端收到空响应。  

- **Error in SGLang multi‑machine PP (Pipeline Parallelism) mode deployment for inference**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18366>  
  - 简述：用户在多机 PP 部署时遇到环境变量配置与启动脚本错误，导致推理失败。  

- **🚀 [RFC] Decoupling Framework Logic from Hardware Kernels: Offloading Triton Kernels to sgl‑kernel**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18365>  
  - 简述：提议将框架层逻辑与硬件 Triton kernel 解耦，迁移至独立的 `sgl‑kernel` 项目，以提升可维护性与跨平台兼容。  

- **[Diffuser] Why connection failed on EOFError ?**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18363>  
  - 简述：用户在使用 Diffuser 进行视频生成时，启动脚本报 EOFError，无法加载模型权重。  

- **[Bug] DeepSeek OCR 2 Accuracy Issue**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18358>  
  - 简述：DeepSeek OCR‑2 在 SGLang 中的识别准确率低于 Transformers 基准，怀疑模型加载或后处理差异。  

- **Sanitize exception messages in HTTP entrypoints to prevent information exposure**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18348>  
  - 简述：建议在 HTTP API 层统一捕获异常，返回通用错误信息并在服务器日志中记录完整堆栈，以避免泄露内部实现细节。  

- **Potential ReDoS in Pythonic tool call detector**  
  - 链接：<https://github.com/sgl-project/sglang/issues/18347>  
  - 简述：`sglang/srt/function_call/pythonic_detector.py` 中的正则表达式存在嵌套重复，可能被恶意输入触发正则拒绝服务（ReDoS）。建议重构为更安全的解析方式。
### vllm-project/vllm
- **[bugfix] [ROCm] Fix premature CUDA initialization in platform detection**  
  PR: <https://github.com/vllm-project/vllm/pull/33941> (SHA `4a2d00e`)  
  关键文件：`vllm/platforms/rocm.py`（新增 40 行，删除 6 行），在平台检测阶段延迟 CUDA 初始化，防止在 ROCm 环境下提前加载 CUDA。  
  其他改动：`.buildkite/test-amd.yaml`、`.buildkite/test_areas/cuda.yaml` 各加 1 行；新增 CUDA 测试脚本 `tests/cuda/scripts/check_device_count_respects_env.py`、`tests/cuda/scripts/check_platform_no_cuda_init.py` 与对应测试 `tests/cuda/test_platform_no_cuda_init.py`（共 111 行新增）。

- **Fix RoutingMethodType logic**  
  PR: <https://github.com/vllm-project/vllm/pull/33919> (SHA `207c3a0`)  
  关键文件：`vllm/model_executor/layers/fused_moe/router/router_factory.py`（新增 14 行，删除 1 行），修正路由方法类型判断；`fused_topk_router.py` 与 `fused_topk_bias_router.py` 各调整 13 行实现；`config.py`、`flashinfer_trtllm_moe.py` 也相应更新。

- **Fix `logprobs=0` handling for `/inference/v1/generate` endpoint**  
  PR: <https://github.com/vllm-project/vllm/pull/34010> (SHA `ae2e93f`)  
  关键文件：`vllm/entrypoints/serve/disagg/serving.py`（修改 5 行），确保 `logprobs=0` 时返回空列表而非错误；对应测试 `tests/entrypoints/openai/test_serving_tokens.py` 增加 26 行断言。

- **Fix no attribute error of SharedFusedMoE (DeepSeek‑V3.1 as test model)**  
  PR: <https://github.com/vllm-project/vllm/pull/33993> (SHA `9e9acce`)  
  关键文件：`vllm/model_executor/layers/fused_moe/shared_fused_moe.py`（新增 4 行），为 `SharedFusedMoE` 添加缺失属性；`layer.py` 小幅调整（+2/-1 行）。

- **[Rocm][Bugfix] Fix dtype not same for gemm_a4w4 op**  
  PR: <https://github.com/vllm-project/vllm/pull/33734> (SHA `fe54382`)  
  关键文件：`vllm/model_executor/layers/quantization/quark/schemes/quark_ocp_mx.py`（新增 6 行，删除 1 行），统一 GEMM A4W4 操作的 dtype。

- **[Refactor] Remove align block size logic in `moe_permute`**  
  PR: <https://github.com/vllm-project/vllm/pull/33449> (SHA `77c09e1`)  
  关键文件：`csrc/moe/moe_permute_unpermute_op.cu`（删除 39 行，新增 5 行），`csrc/moe/permute_unpermute_kernels/moe_permute_unpermute_kernel.cu`（删除 60 行），以及对应的头文件和测试 `tests/kernels/moe/test_moe_permute_unpermute.py`（删除 118 行，新增 15 行），简化 permute 逻辑并提升编译速度。

- **[Model Runner V2] support apply penalty for spec decode**  
  PR: <https://github.com/vllm-project/vllm/pull/33251> (SHA `16786da`)  
  关键文件：`vllm/v1/worker/gpu/sample/penalties.py`（新增 37 行，删除 7 行），在 Spec‑Decode 中加入惩罚项；`model_runner.py`、`input_batch.py`、`sampler.py` 也同步更新以支持新惩罚。

- **[DOC] [ROCm] Update docker deployment doc**  
  PR: <https://github.com/vllm-project/vllm/pull/33971> (SHA `aaa2efb`)  
  关键文件：`docs/deployment/docker.md`（删除 156 行，新增 8 行），重新组织 ROCm Docker 部署说明；`gpu.cuda.inc.md`、`gpu.rocm.inc.md`、`gpu.md` 也相应更新。

- **[KV Connector] Add missing method overrides to MultiConnector**  
  PR: <https://github.com/vllm-project/vllm/pull/33292> (SHA `aca5967`)  
  关键文件：`vllm/distributed/kv_transfer/kv_connector/v1/multi_connector.py`（新增 43 行，删除 5 行），实现缺失的抽象方法；对应单元测试 `tests/v1/kv_connector/unit/test_multi_connector.py` 增加 134 行覆盖。

- **[Log] Optimize duplicate startup log**  
  PR: <https://github.com/vllm-project/vllm/pull/33944> (SHA `67a746e`)  
  关键文件：`vllm/compilation/backends.py`、`vllm/utils/deep_gemm.py`、`vllm/v1/worker/gpu_worker.py` 各做少量行数的去重处理，减少启动时的重复日志。

- **[Bugfix] Fix tool calling with fast detokenization (dsv32)**  
  PR: <https://github.com/vllm-project/vllm/pull/33964> (SHA `7bec435`)  
  关键文件：`vllm/tool_parsers/deepseekv32_tool_parser.py`（新增 12 行），修复在使用 dsv32 快速去标记时工具调用失效的问题。

- **[Docs] Update link to Benchmark CLI documentation**  
  PR: <https://github.com/vllm-project/vllm/pull/33254> (SHA `5c52644`)  
  关键文件：`benchmarks/README.md`（修改 1 行），更新 Benchmark CLI 的链接。

- **[XPU][5/N] add wna16 xpu kernel**  
  PR: <https://github.com/vllm-project/vllm/pull/33973> (SHA `2ce9fe4`)  
  关键文件：`vllm/model_executor/layers/quantization/kernels/mixed_precision/xpu.py`（新增 57 行，删除 66 行），实现 wna16 精度的 XPU kernel；CI 脚本 `.buildkite/scripts/hardware_ci/run-xpu-test.sh` 也作相应调用。

- **[Refactor] Consolidate sequence normalization and enc‑dec parsing**  
  PR: <https://github.com/vllm-project/vllm/pull/33928> (SHA `cd8b405`)  
  关键文件：`vllm/engine/protocol.py`（新增 7 行，删除 2 行），统一序列归一化与编码‑解码解析；`vllm/entrypoints/llm.py` 大幅改动（+187/-149 行），提升入口参数处理的可维护性。

- **[Model] Support MiniCPM‑o 4.5**  
  PR: <https://github.com/vllm-project/vllm/pull/33431> (SHA `4707f7e`)  
  关键文件：`vllm/model_executor/models/minicpmo.py`（新增 86 行，删除 7 行），加入 MiniCPM‑o 4.5 的模型适配。

- **[Docs] Add sections on process architecture and minimum CPU resources**  
  PR: <https://github.com/vllm-project/vllm/pull/33940> (SHA `c39ee9e`)  
  关键文件：`docs/design/arch_overview.md`（新增 67 行），`docs/configuration/optimization.md`（新增 46 行），并新增两张架构示意图。

- **[ROCm][AITER] Fix AITER import regression for explicit backend selection**  
  PR: <https://github.com/vllm-project/vllm/pull/33749> (SHA `350ca72`)  
  关键文件：`vllm/_aiter_ops.py`（新增 114 行，删除 34 行），`vllm/v1/attention/backends/rocm_aiter_fa.py`（微调 9 行），确保在显式指定后端时 AITER 正常导入。

- **[FIX] guidance: use max(vocab_size, len(tokenizer)) for n_vocab**  
  PR: <https://github.com/vllm-project/vllm/pull/33509> (SHA `1fb0495`)  
  关键文件：`vllm/v1/structured_output/backend_guidance.py`（+1/-1 行），防止 vocab 大小低于 tokenizer 长度导致错误。

- **[Bugfix] Fix models and tests for transformers v5**  
  PR: <https://github.com/vllm-project/vllm/pull/33977> (SHA `85ee1d9`)  
  关键文件：`vllm/model_executor/models/audioflamingo3.py`、`hunyuan_vision.py`、`isaac.py` 等均做微调（+/-1 行），并更新多模态测试以兼容 Transformers 5.0。

- **Update `WeightTransferConfig` to be more standard**  
  PR: <https://github.com/vllm-project/vllm/pull/33989> (SHA `51a7bda`)  
  关键文件：`vllm/config/weight_transfer.py`（删除 2 行），`vllm/engine/arg_utils.py`（+4/-4 行），统一配置类的命名与结构。

- **[Docs] Improve documentation**  
  PR: <https://github.com/vllm-project/vllm/pull/33799> (SHA `6e7b1c4`)  
  关键文件：`docs/models/supported_models.md`（增删各 8 行），`vllm/config/model.py`、`vllm/entrypoints/llm.py` 各微调 2 行，提升文档可读性。

- **[Bugfix][Model] Support LoRA on Qwen3 Output Embedding**  
  PR: <https://github.com/vllm-project/vllm/pull/29816> (SHA `2991dd3`)  
  关键文件：`vllm/model_executor/models/qwen3.py`、`qwen3_moe.py`（各 +5 行），`vllm/lora/layers/logits_processor.py`（+5/-2 行），并新增 LoRA 单元测试 `tests/lora/test_qwen3_unembed.py`（100 行）。

- **[torch.compile] Reorganize compilation tests**  
  PR: <https://github.com/vllm-project/vllm/pull/33731> (SHA `ac32e66`)  
  关键文件：`tests/compile/correctness_e2e/test_async_tp.py`（新增 79 行），`tests/compile/passes/distributed/test_async_tp.py`（重命名并改动 80 行），整体重构 `vllm/compilation` 目录结构以支持 vLLM IR。

- **[CPU][BugFix] Fix loading of w8a8int models with bias**  
  PR: <https://github.com/vllm-project/vllm/pull/33582> (SHA `f79d9dc`)  
  关键文件：`vllm/model_executor/layers/quantization/kernels/mixed_precision/dynamic_4bit.py`（+8/-3 行），修复带 bias 的 8‑bit 模型加载错误。

- **Bump HF Hub client to get bug fix**  
  PR: <https://github.com/vllm-project/vllm/pull/33984> (SHA `ba5cbbf`)  
  关键文件：`requirements/rocm-test.txt`、`requirements/test.txt`（各 +1/-1 行），升级 `huggingface_hub` 版本。

- **[PaddleOCR‑VL] Add BC for transformers 5.0 config**  
  PR: <https://github.com/vllm-project/vllm/pull/33976> (SHA `233b26a`)  
  关键文件：`vllm/model_executor/models/paddleocr_vl.py`（新增 7 行），兼容 Transformers 5.0 的配置格式。

- **Consolidate and fix forbidden import `pre‑commit` checks**  
  PR: <https://github.com/vllm-project/vllm/pull/33982> (SHA `791a94b`)  
  关键文件：`.pre-commit-config.yaml`（修改 21 行），新增 `tools/pre_commit/check_forbidden_imports.py`（142 行），删除旧的检查脚本，提升 CI 对禁用导入的检测。

- **support view_from_cpu_tensor on XPU**  
  PR: <https://github.com/vllm-project/vllm/pull/33868> (SHA `e969a16`)  
  关键文件：`vllm/utils/torch_utils.py`（+6/-2 行），`vllm/v1/worker/gpu/buffer_utils.py`（+2/-2 行），为 XPU 添加 `view_from_cpu_tensor` 支持。

- **Fix `main` pre‑commit**  
  PR: <https://github.com/vllm-project/vllm/pull/33975> (SHA `6d8d34b`)  
  关键文件：`vllm/model_executor/models/voyage.py`（+1/-1 行），修正 pre‑commit 钩子中的 `main` 引用。

- **[cpu][performance] CPU Paged Attention NEON BFMMLA BF16 Implementation**  
  PR: <https://github.com/vllm-project/vllm/pull/32263> (SHA `1363e3d`)  
  关键文件：`csrc/cpu/cpu_attn_neon_bfmmla.hpp`（新增 682 行），实现基于 NEON BFMMLA 的 BF16 注意力加速；`cpu_attn_impl.hpp`、`cpu_attn_neon.hpp` 也相应更新。

- **Onboard voyage‑4‑nano**  
  PR: <https://github.com/vllm-project/vllm/pull/33720> (SHA `9655256`)  
  关键文件：`vllm/model_executor/models/voyage.py`（新增 130 行），加入 Voyage‑4‑Nano 模型支持；文档 `docs/models/supported_models.md` 更新。

- **[XPU] Replace pip in docker.xpu with uv pip**  
  PR: <https://github.com/vllm-project/vllm/pull/31112> (SHA `6550815`)  
  关键文件：`docker/Dockerfile.xpu`（新增 46 行，删除 28 行），改用 `uv` 加速依赖安装。

---

### Issues（截至 2026‑02‑06）

- **[Usage] How to load FP8 Quantised Model - Value error, Found unknown quantization**  
  <https://github.com/vllm-project/vllm/issues/34028> – 作者 *gabinguo*。  
  Issue 描述缺少完整的 `collect_env.py` 输出，正文被截断，需补全环境信息以定位 FP8 量化加载错误。

- **[RFC] Helix (Context + Tensor) Parallelism for Efficient Long‑Context Decoding**  
  <https://github.com/vllm-project/vllm/issues/34018> – 作者 *sungsooha*。  
  提议实现 Helix 并行方案，结合 Context Parallelism 与 Tensor Parallelism，减少 KV 缓存复制，提升长上下文解码效率。

- **[Bug] vLLM crashes when trying to load Devstral‑2‑123B‑Instruct‑2512 directly from S3**  
  <https://github.com/vllm-project/vllm/issues/34016> – 作者 *moorglade*。  
  Issue 内容为空，仅报告崩溃，需要提供堆栈信息和 S3 路径以复现。

- **[Feature] MXFP8 GEMM / Grouped GEMM Kernels for AMD**  
  <https://github.com/vllm-project/vllm/issues/34012> – 作者 *EdalatiAli*（1 条
### NVIDIA/cutile-python
昨日无更新。

## 总结
### 展望与关注点
- **硬件适配**：FlashInfer 与 vLLM 对 SM120/SM121 的 FP4 tile 与 Stream‑K 支持，以及 XPU wna16 kernel 的落地，预示着新一代 GPU/加速卡的算子库正快速跟进，后续需关注这些特性在实际推理工作负载中的稳定性与兼容性。\
- **性能瓶颈**：CUTLASS 社区报告的 GEMM+SiLU 延迟问题提醒我们，复合算子（尤其是带有非线性 epilogue）的实现仍是性能优化的热点，期待后续 upstream 修复。\
- **CI 与可维护性**：SGLang 与 vLLM 大幅度清理 CI、文档与 pre‑commit 规则，提升了项目的可维护性和跨平台部署体验，值得其他开源 AI 基础设施项目借鉴。\
- **模型生态**：vLLM 新增 MiniCPM‑o、Voyage‑4‑nano 等模型支持，配合 FlashInfer 的 API 重构，为多模型部署提供了更统一的接口，建议关注这些模型在实际业务场景中的表现与调优指南。\
- **下一步**：关注 FlashInfer 的 SM120/SM121 性能基准、vLLM 对 ROCm/CPU 新算子的实际加速效果，以及 CUTLASS 对 GEMM+SiLU 的后续优化进展，以便及时评估对自研推理框架的迁移价值。
