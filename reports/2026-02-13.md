今日的 AI Infra 的新闻如下。

## 摘要
## 本周关键变更概览

- **NVIDIA/cutlass**：4.4.0 正式发布，CuTeDSL 引入 CTA 级别示例并支持 **LayerNorm / RMSNorm**，`fence_proxy` 语法简化为字符串，TVM FFI 初始化默认流 `-1`，新增 **AoT 编译、JAX 支持、实验性 CUDA 13.1 / GB300** 等特性，并同步更新大量示例代码（如 `cta_norm.py`、`cooperative_launch.py`）。

- **flashinfer-ai/flashinfer**：修复类型不匹配 bug，**AOT JIT 缓存**加入 `fp8_blockscale_gemm_90` 与 `gen_gemm_sm100_module_cutlass_mxfp8`，以及 **CUDA Graph** 采样模块的稳健性提升。

- **Dao-AILab/flash-attention**：大幅 DSL 重构，`ParamsBase` 统一参数管理，SM90/SM100 后向核实现 **大幅宽度重构** 与 **dK/dV R2S** 优化，调度器逻辑简化，整体代码行数净减少约 300 行。

- **sgl-project/sglang**：本周提交量最高，涉及 **VAE `channels_last_3d` 优化、fast‑hadamard‑transform JIT、Spec V2 开关、MROPE/YARN 支持、NSA 多规格后端融合、AMD/ROCm 兼容性、分布式初始化变量、暂停/恢复 API、以及大量 CI/性能基准更新**，代码改动累计超过 13 k 行，显著提升多模态、扩散与大模型推理的吞吐与可维护性。

- **vllm-project/vllm**：聚焦 **Mamba 前缀缓存对齐、输入预处理迁入渲染层、ROC m 兼容性防护、Qwen3.5 配置修复、COLQwen3 多模态模型、Spec 解码在对齐模式下启用、Pipeline‑Parallel 异步通信提升 2.9% 吞吐、CPU Offload 双分配优化、KV 连接器精简、暂停/恢复迁入引擎层**，并同步大量测试与示例。

- **NVIDIA/cutile-python**：在 CUDA Tile 编译上下文中加入 **临时目录自动创建**，提升脚本鲁棒性。

## 影响与趋势
- **DSL 与 AOT/JIT 结合**：Cutlass、FlashAttention 与 vLLM 均在 DSL 层面加入 AoT/JIT 缓存，降低运行时编译开销。
- **新硬件适配**：CUDA 13.1、SM100/SM120、AMD ROCm 等平台的专属优化持续加速，尤其在注意力、MoE 与混合精度场景。
- **多模态与扩散**：SGLang 与 FlashInfer 大幅强化 VAE、MROPE、Diffusion 相关实现，表明行业对视觉‑语言、生成式模型的需求上升。
- **调度与并行**：vLLM 的 Pipeline‑Parallel 异步通信、Mamba 前缀缓存对齐、SGLang 的分布式初始化变量等，凸显对大规模推理调度的深度优化。
- **CI 与可维护性**：多项目加入 ROCm 兼容防护、CI 并发策略、文档链接检查等，提升发布质量与跨平台稳定性。

## 具体内容分析
### deepseek-ai/DeepGEMM
昨日无更新。
### deepseek-ai/FlashMLA
昨日无更新。
### NVIDIA/cutlass
## 提交

| SHA | PR | 作者 | 日期 | 简要描述 |
|-----|----|------|------|----------|
| [291300f](https://github.com/NVIDIA/cutlass/commit/291300ffffa3533a78ee104f08a8490a29ce9ccb) | [#3009](https://github.com/NVIDIA/cutlass/pull/3009) | Yihan Chen | 2026‑02‑14 | 在 CuTeDSL 中实现了 CTA 级别的归一化示例，支持 LayerNorm 与 RMSNorm。 |
| [f9a5f76](https://github.com/NVIDIA/cutlass/commit/f9a5f76b7a4f4298d4ec4039c9ae2a673c9fab93) | [#3027](https://github.com/NVIDIA/cutlass/pull/3027) | aragorn‑guan | 2026‑02‑14 | 将 `fence_proxy` 调用简化为字符串形式，提升代码可读性。 |
| [ec7e6cb](https://github.com/NVIDIA/cutlass/commit/ec7e6cb17be771ee502e96f16ec923843c12c6d0) | 合并 PR #2971 | drazi | 2026‑02‑14 | TVM FFI 初始化改为 `tvm_ffi.from_dlpack(tensor, stream=-1)`，显式指定默认流。 |
| [395ab57](https://github.com/NVIDIA/cutlass/commit/395ab575f659a82a26dc577e0314017758f07d42) | 合并分支 `tv‑m‑ffi` | Yuan Xiaolan | 2026‑02‑14 | 更新 CHANGELOG 与 README，标记 4.4.0 正式发布并列出 CuTeDSL 新特性（CUDA 13.1、GB300、experimental API 等）。 |
| [d4bbf72](https://github.com/NVIDIA/cutlass/commit/d4bbf728ca52ce23a549a299e229666e734ffb7c) | [#3032](https://github.com/NVIDIA/cutlass/pull/3032) | Junkai‑Wu | 2026‑02‑14 | 4.4.0 发行说明细化，新增实验性示例、AoT 编译、JAX 支持等。 |
| [01687cf](https://github.com/NVIDIA/cutlass/commit/01687cfba1c7d27c68b7b3c5cca3b5b4654e0da7) | 合并 PR #3004 | drazi | 2026‑02‑13 | 为 `sub_packed_f32x2` 添加架构层支持；在 `arch/__init__.py` 与 `arch/nvvm_wrappers.py` 中加入少量代码。 |
| [5c42d0f](https://github.com/NVIDIA/cutlass/commit/5c42d0f28c9dc728d874fd552492a355036a53e1) | 合并 PR #3021 | drazi | 2026‑02‑13 | 在 `arch/clc.py` 中去除多播相关实现，简化 CLC 指令路径。 |
| [1d36152](https://github.com/NVIDIA/cutlass/commit/1d36152f34c06bce45cf196269c08a78a71079aa) | 合并 PR #3022 | drazi | 2026‑02‑13 | 为 `nvvm_wrappers.py` 添加 `fmin` 包装函数，提升 DSL 对最小值操作的支持。 |

### 关键文件变更

- **examples/python/CuTeDSL/hopper/cta_norm.py** (新增)  
  ```python
  # Copyright (c) 2025 - 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
  ```
- **examples/python/CuTeDSL/distributed/all_reduce_tma.py** (修改)  
  ```diff
  - cute.arch.fence_proxy(
  -     cute.arch.ProxyKind.async_shared,
  -     space=cute.arch.SharedSpace.shared_cta,
  - )
  + cute.arch.fence_proxy("async.shared", space="cta")
  ```
- **python/CuTeDSL/cutlass/cute/runtime.py** (修改)  
  ```diff
  - self._tvm_ffi_tensor = tvm_ffi.from_dlpack(tensor)
  + self._tvm_ffi_tensor = tvm_ffi.from_dlpack(tensor, stream=-1)
  ```
- **CHANGELOG.md** (修改) – 章节标题更新为正式发布链接，列出 CuTeDSL 对 CUDA 13.1、GB300、experimental API 等的支持。  
  *注：diff 过长，仅展示前几行。*
- **README.md** (修改) – 同上，更新版本时间戳并同步新特性列表。  
  *注：diff 过长，仅展示前几行。*
- **examples/python/CuTeDSL/ampere/cooperative_launch.py** (新增)  
  ```python
  # Copyright (c) 2025 - 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
  ```
- **examples/python/CuTeDSL/ampere/elementwise_add.py** (修改) – 为 kernel 设置名称前缀：  
  ```python
  kernel_name = f"cutlass_dsl_elementwise_add_kernel"
  elementwise_add_kernel.set_name_prefix(kernel_name)
  ```
- **examples/python/CuTeDSL/ampere/elementwise_add_autotune.py** (新增) – 同上，包含完整示例代码（省略）。  
- **examples/python/CuTeDSL/ampere/elementwise_apply.py** (修改) – 在 `leaky_relu_ref` 中局部导入 `torch`，并在 `run_and_verify` 中显式导入 `torch` 与 `cutlass.torch`。  
- **examples/python/CuTeDSL/ampere/flash_attention_v2.py** (修改) – 在 `run` 与 `create_tensor` 中加入 `import torch` 与 `cutlass.torch`，并演示编译选项传递。  
- **examples/python/CuTeDSL/ampere/hstu_attention.py** (修改) – 移除显式的 `torch` 类型注解，改为运行时导入；更新文档描述。  
- **examples/python/CuTeDSL/ampere/inline_ptx.py** (修改) – 文档中将 “nvvm dialect” 表述统一为 “nvvm dialect”。  
- **examples/python/CuTeDSL/ampere/sgemm.py** (修改) – 在 `run` 函数顶部加入 `import torch`，并在入口处保留 `torch.manual_seed(1024)`。  
- **examples/python/CuTeDSL/ampere/smem_allocator.py** (修改) – 在 `run_and_verify` 中局部导入 `torch`，保持 GPU Tensor 初始化。  
- **python/CuTeDSL/cutlass/cute/arch/__init__.py** (修改) – 新增一行导入（具体代码片段未显示，diff 仅 1 行添加）。  
- **python/CuTeDSL/cutlass/cute/arch/nvvm_wrappers.py** (修改) – 新增 `fmin` 包装函数（14 行新增代码）。  
- **python/CuTeDSL/cutlass/cute/arch/clc.py** (修改) – 删除多播相关实现，保留 15 行新增、6 行删除的代码。  

> **说明**：若 `patch_truncated` 为 `true`，表示完整 diff 未展示，仅提供关键片段或概述。

## Issues

| 编号 | 标题 | 链接 | 作者 | 创建时间 |
|------|------|------|------|----------|
| #3031 | **[FEA] Assume leading dim when size 1 =** | https://github.com/NVIDIA/cutlass/issues/3031 | drisspg | 2026‑02‑13 |
| #3029 | **[FEA] Add CopyReduceBulkS2G** | https://github.com/NVIDIA/cutlass/issues/3029 | tridao | 2026‑02‑13 |

*两条 Issue 均为 CuTeDSL 功能需求，分别涉及动态布局推断与新增 `CopyReduceBulkS2G` 指令封装。*
### flashinfer-ai/flashinfer
**提交记录**  

- **f4d10a7** – *bugfix*: 修复 #2507 中提到的枚举/整数类型不匹配。  
  - 作者：Zihao Ye 日期：2026‑02‑14 17:26:34 UTC [PR 链接](https://github.com/flashinfer-ai/flashinfer/commit/f4d10a7dca0082f45ac8010bf3a798f90f3d05a6)  
  - 变更文件：  
    - `flashinfer/fused_moe/core.py`（修改 4 行，删除 4 行）  
    - `tests/moe/test_trtllm_gen_fused_moe.py`（修改 8 行，删除 8 行）  
  - 说明：diff 已被截断，具体代码片段未显示。

- **292f9be** – *fix*: 将 `fp8_blockscale_gemm_90` 纳入 AOT JIT‑cache。  
  - 作者：Edward 日期：2026‑02‑13 04:29:43 UTC [PR 链接](https://github.com/flashinfer-ai/flashinfer/commit/292f9be3f5f6d76248d4d3577c167fd178d7952d)  
  - 变更文件：  
    - `flashinfer/aot.py`（新增 3 行）  
  - 说明：仅添加了对新 kernel 的注册代码，diff 已被截断。

- **c5b8a2e** – *fix*: Sampling 模块的 CUDA Graph 修复。  
  - 作者：Izzy Putterman 日期：2026‑02‑13 00:55:46 UTC [PR 链接](https://github.com/flashinfer-ai/flashinfer/commit/c5b8a2e0e2a54e115f80fa6d096000536243c6ec)  
  - 变更文件：  
    - `csrc/flashinfer_sampling_binding.cu`（+19 ‑8）  
    - `csrc/sampling.cu`（+104 ‑16）  
    - `flashinfer/sampling.py`（+291 ‑76）  
    - `include/flashinfer/sampling.cuh`（+82 ‑37）  
  - 说明：大幅度重构了采样绑定与 CUDA Graph 逻辑，diff 已被截断。

- **2fff6b6** – *feat*: 将 `gen_gemm_sm100_module_cutlass_mxfp8` 加入 JIT‑cache。  
  - 作者：Yong Wu 日期：2026‑02‑13 00:25:54 UTC [PR 链接](https://github.com/flashinfer-ai/flashinfer/commit/2fff6b6a3791bd1abf9a7e829ab3096a563dc86b)  
  - 变更文件：  
    - `flashinfer/aot.py`（新增 2 行）  
  - 说明：仅在 AOT 注册表中添加了对应模块入口，diff 已被截断。

---

**Issues**  

- **griddepcontrol.wait should use "memory" clobber**  
  - URL: <https://github.com/flashinfer-ai/flashinfer/issues/2558>  
  - 作者：MatthiasKohl 创建时间：2026‑02‑13 10:08:59 UTC  
  - 简述：指出 `griddepcontrol.wait` 缺少 `"memory"` clobber，可能导致未定义行为；建议直接使用 `cudaTriggerProgrammaticLaunchCompletion` 与 `cudaGridDependencySynchronize`。  
  - 备注：正文被截断，详细讨论未展示。

- **`trtllm_batch_context_with_kv_cache` uses SM103 cubin but not `trtllm_batch_decode_with_kv_cache`**  
  - URL: <https://github.com/flashinfer-ai/flashinfer/issues/2556>  
  - 作者：b8zhong 创建时间：2026‑02‑13 02:47:28 UTC  
  - 简述：询问 SM103 变体在 decode 阶段缺失的原因，关注 FP8 Q/K/V 与 BF16 输出的兼容性。  
  - 备注：正文被截断，细节未完整呈现。

- **SM120 attention kernels exist but are blocked by wiring issues (fmha_v2, backend selector, MLA)**  
  - URL: <https://github.com/flashinfer-ai/flashinfer/issues/2555>  
  - 作者：blake-snc 创建时间：2026‑02‑13 01:44:39 UTC  
  - 简述：发现 SM120 注意力 kernel 已实现但未在运行时路径中启用，列出三处环境变量或条件判断导致的屏蔽。  
  - 备注：正文被截断，具体代码位置与建议修复方案仅部分可见。
### Dao-AILab/flash-attention
**提交**  

- `ad2f470` – [查看提交](https://github.com/Dao-AILab/flash-attention/commit/ad2f4702bdb26d15b630bb8b11a69ced671d7a74)  
  - **flash_attn/cute/cute_dsl_utils.py** – 删除 24 行（无新增），实现 `ParamsBase` 的 DSL 重构。  
  - **flash_attn/cute/flash_bwd.py** – +2 / -1 行，微调后向传播入口。  
  - **flash_attn/cute/flash_bwd_postprocess.py** – +1 / -1 行，后处理细节调整。  
  - **flash_attn/cute/flash_bwd_preprocess.py** – +1 / -1 行，前处理细微改动。  
  - **flash_attn/cute/flash_bwd_sm100.py** – +1 / -1 行，SM100 版后向代码小幅更新。  
  - **flash_attn/cute/flash_bwd_sm90.py** – +2 / -1 行，SM90 版后向代码小幅更新。  
  - **flash_attn/cute/flash_fwd.py** – +1 / -1 行，前向入口微调。  
  - **flash_attn/cute/flash_fwd_sm100.py** – +1 / -1 行，SM100 前向代码微调。  
  - **flash_attn/cute/paged_kv.py** – +1 / -1 行，分页 KV 实现细节修改。  
  - **flash_attn/cute/pyproject.toml** – +1 行，构建配置微调。  
  - **flash_attn/cute/softmax.py** – +1 / -1 行，softmax 实现小改。  
  - **flash_attn/cute/tile_scheduler.py** – +4 / -26 行，调度器逻辑大幅简化。  

- `16d16d8` – [查看提交](https://github.com/Dao-AILab/flash-attention/commit/16d16d8cba2cc967123e8c6efd3b67e90b9cf2d5)  
  - **flash_attn/cute/block_sparse_utils.py** – +2 / -1 行，块稀疏工具函数微调。  
  - **flash_attn/cute/flash_bwd_postprocess.py** – +4 / -2 行，后处理逻辑简化。  
  - **flash_attn/cute/flash_bwd_preprocess.py** – +5 / -2 行，前处理逻辑简化。  
  - **flash_attn/cute/flash_bwd_sm90.py** – +79 / -132 行，核心后向实现大幅重构，简化 dK/dV R2S 拷贝。  
  - **flash_attn/cute/flash_fwd_sm100.py** – +2 / -1 行，SM100 前向代码小幅更新。  
  - **flash_attn/cute/pyproject.toml** – +1 / -1 行，构建配置微调。  

> **说明**：所有文件的 `patch` 均被截断（`patch_truncated: true`），因此未展示具体代码片段。如需查看完整改动，请访问对应的提交链接。

**Issues**  
- 本次提交未关联任何 Issue。

**仓库整体变动概览**  
- **总计**：+109 行，-198 行，修改 307 行。  
- 主要聚焦在 **flash_attn/cute** 子模块，涉及前向/后向核心实现、调度器、DSL 参数基类以及构建配置。  
- 关键改动：  
  - 引入 `ParamsBase` 统一 DSL 参数管理。  
  - 大幅简化 SM90 后向实现中的 dK/dV R2S 拷贝逻辑，提升可读性与维护性。  
  - `tile_scheduler.py` 中的调度策略被精简，删除冗余代码。  
  - 多个文件的前后处理入口做了细微的行数调整，以配合上述重构。  

这些改动整体提升了代码结构的清晰度，并为后续在不同 GPU 架构（SM90/SM100）上的优化奠定了更好的基础。
### sgl-project/sglang
- **[diffusion] feat: opt vae decode with `channels_last_3d`**  
  PR: <https://github.com/sgl-project/sglang/commit/4067d9487d371eb4e29d626424bd0e1edc8c3ec8>  
  - 关键文件: `python/sglang/multimodal_gen/envs.py`（+4 行）; `python/sglang/multimodal_gen/runtime/loader/component_loaders/vae_loader.py`（+41 行）  
  - 实现 VAE 解码的 `channels_last_3d` 优化，提升多模态生成效率。  

- **[kernel slimming] Move fast_hadamard_transform to jit_kernel**  
  PR: <https://github.com/sgl-project/sglang/commit/c29394e3c84a61e160da78d2edbf11939ec0ec07>  
  - 新增 `fast-hadamard-transform` 目录，包含 C++/CUDA 实现、代码生成脚本以及 Python 包装 (`python/sglang/jit_kernel/hadamard.py`)。  
  - 相关文件总计约 2 241 行改动（+2 170 行），实现 JIT 编译的快速 Hadamard 变换。  

- **Enable SGLANG_ENABLE_SPEC_V2 for nightly speculative decoding tests**  
  PR: <https://github.com/sgl-project/sglang/commit/ae958692927509723ada6e33f368bdba148f1654>  
  - 修改测试工具 (`accuracy_test_runner.py`, `nightly_utils.py`, `performance_test_runner.py`, `test_utils.py`) 以及多模型注册脚本，开启 Spec V2 选项。  

- **feat: Support `mrope_section` with `rope_type: "yarn"`**  
  PR: <https://github.com/sgl-project/sglang/commit/92cdd398cd599b65c42dfa274353e70e0ce668c4>  
  - `python/sglang/srt/layers/rotary_embedding.py`（+101 行，-10 行）加入对 MROPE/YARN 的兼容。  
  - 更新对应测试 `test/registered/rotary/test_mrope.py`（+27 行，-2 行）。  

- **Add ci test for ring model**  
  PR: <https://github.com/sgl-project/sglang/commit/f51e9d9ca1a6966c88e93974b2749893ad1c8a39>  
  - 新增环形模型测试 `test/registered/8-gpu-models/test_ring_2_5_1t.py`（+53 行）。  
  - 调整 `accuracy_test_runner.py` 以支持新测试。  

- **Fix dsv32 encode_messages**  
  PR: <https://github.com/sgl-project/sglang/commit/c8aa2a65341aaa4a1b23fa7ee734ace4278e37c8>  
  - `python/sglang/srt/entrypoints/openai/serving_chat.py`（+14 行）修复编码错误。  
  - `python/sglang/srt/parser/jinja_template_utils.py`（+16 行，-5 行）同步更新。  

- **Kernel: optimize decoding metadata in NSA multi-spec backend with fused kernels**  
  PR: <https://github.com/sgl-project/sglang/commit/34132d6da50e0867426962d3681b62a03b5624b9>  
  - 新增融合元数据拷贝实现：`fused_metadata_copy.cuh`（+722 行）和 `fused_metadata_copy.py`（+316 行）。  
  - 对 NSA 后端相关文件进行大量改动（总计约 2 878 行），提升多规格推理的元数据处理效率。  

- **[AMD] Fix sgl-model-gateway Build Errors in ROCm Docker Release**  
  PR: <https://github.com/sgl-project/sglang/commit/38473f8ee06ac5b6f2074d8bc1f84f88ee7a9da0>  
  - 调整 CI 工作流文件和 `sgl-model-gateway/Cargo.toml`（各 +1 / -1 行），解决 ROCm Docker 构建问题。  

- **[DLLM] Update CODEOWNERS for diffusion LLM**  
  PR: <https://github.com/sgl-project/sglang/commit/e2eb5bf28d5f06b1e2ee857531cf52648e62b187>  
  - 在 `.github/CODEOWNERS` 中新增一行所有者声明。  

- **[VLM][LLM] Optimize fused_moe triton kernel tma**  
  PR: <https://github.com/sgl-project/sglang/commit/fa0ef6e4f7f7c8937e498d8db6fa8599405d2a18>  
  - `fused_moe_triton_kernels.py`（+67 行，-8 行）加入 TMA 优化，提升 MoE 推理吞吐。  

- **Fix/partial gen from waiting queue miss metadata**  
  PR: <https://github.com/sgl-project/sglang/commit/f6c18c3a85a9f9e7b388e849bedced97896522f1>  
  - `tokenizer_manager.py`（+7 行，-1 行）修复等待队列元数据缺失导致的部分生成错误。  
  - 更新 `test/registered/scheduler/test_abort.py`（+33 行，-12 行）对应测试。  

- **[diffusion][MUSA] fix: MUSA platform breakage**  
  PR: <https://github.com/sgl-project/sglang/commit/45a4697d452d294c591c38742491cc478d31c80a>  
  - `python/sglang/multimodal_gen/runtime/platforms/musa.py`（+8 行）修复 MUSA 兼容性。  

- **Fix CI concurrency collision between scheduled runs and fork PRs**  
  PR: <https://github.com/sgl-project/sglang/commit/8ef3e3d56be5461de223297dd186bb5fe1b9d11f>  
  - `.github/workflows/pr-test.yml`（+4 行，-3 行）调整并发策略。  

- **Handle abort for retracted requests in disagg decode prealloc queue**  
  PR: <https://github.com/sgl-project/sglang/commit/066b0b70d9803fa3668453bb6aa97e86c7bc330c>  
  - `scheduler.py`（+14 行）加入对已撤回请求的安全中止处理。  

- **[Env] centralize hicache vars in environ.py**  
  PR: <https://github.com/sgl-project/sglang/commit/bd39de7d5e468197ddf2907cd372ebb1e853cb19>  
  - `environ.py`（+2 行）统一 HICACHE 环境变量。  
  - 相关缓存实现文件略有微调（+2 行，-1 行）。  

- **Add timeout abort kits for normal / eagle**  
  PR: <https://github.com/sgl-project/sglang/commit/dcea74d63fed017e1e8e76091702a02f1f3de761>  
  - 新增 `abort_timeout_kit.py`（+144 行）用于超时中止测试。  
  - 调整 `test/registered/scheduler/test_abort.py`（-63 行，+3 行）以及 Eagle 相关测试。  

- **[PD-Disagg] Fix double free when prebuilt batch is aborted**  
  PR: <https://github.com/sgl-project/sglang/commit/4474fb98b435cf6ebcd6b1e61d9f9463834609b6>  
  - `decode_schedule_batch_mixin.py`（+6 行，-5 行）修复双重释放错误。  

- **feat: add SGLANG_DISTRIBUTED_INIT_METHOD_OVERRIDE env var**  
  PR: <https://github.com/sgl-project/sglang/commit/ab0fb248fdd170b527fde68b61a6f36dbf81f688>  
  - `environ.py`（+3 行）新增环境变量说明。  
  - `model_runner.py`（+8 行，-1 行）实现覆盖初始化方法。  

- **[Perf] refactor piecewise cuda graph support of Qwen3-Next**  
  PR: <https://github.com/sgl-project/sglang/commit/8be18c655d0ff1c2e673395102c97c9d5cbd7d1c>  
  - 关键改动涉及 `layernorm_gated.py`（+13 行，-1 行）和 `radix_linear_attention.py`（+61 行，-7 行），优化 Qwen3‑Next 的 CUDA 图支持。  

- **Update performance dashboard for nightly tests**  
  PR: <https://github.com/sgl-project/sglang/commit/3a1c388b43a371e282a82a8c56b88cd8541b4092>  
  - 前端文件 `app.js`, `index.html` 以及后端 `server.py` 大幅更新（共 +1 160 行），提升仪表盘可视化与数据展示。  

- **[CI] feat: add early exit to wait_for_server when process dies**  
  PR: <https://github.com/sgl-project/sglang/commit/3299c4f9c1735c17305175ce1685d0ddbc2e7f91>  
  - 多个 Notebook 示例文件微调（+/-5 行），加入对服务器进程异常的早期退出检测。  

- **[CI] Revive 8‑GPU trace upload in nightly test workflow**  
  PR: <https://github.com/sgl-project/sglang/commit/eccf875d4904a37dc4f6c72aafc254a0e1f1f452>  
  - `nightly-test-nvidia.yml` 增加 38 行步骤；`publish_traces.py` 调整 32 行新增、17 行删除，用于上传运行轨迹。  

- **[FlashInfer] Bump FlashInfer version from 0.6.2 to 0.6.3**  
  PR: <https://github.com/sgl-project/sglang/commit/1be41e9036e19fc620d39e8f7b22572fa36751a9>  
  - 更新 CI、Docker、`pyproject.toml` 等配置文件（每处 +1/-1 行），同步 FlashInfer 依赖。  

- **Update notified user in post_ci_failures_to_slack.py**  
  PR: <https://github.com/sgl-project/sglang/commit/710d873ba6f3fc47e3af3ef7f6c219f396b98238>  
  - 微调 Slack 通知脚本（+1 行，-1 行），改进失败报告的接收人。  

- **fix double-free kv cache for requests that have already finished**  
  PR: <https://github.com/sgl-project/sglang/commit/191d354f538f81ea383e536f3b4421ed441374d8>  
  - `schedule_policy.py`（+9 行，-1 行）防止已完成请求的 KV 缓存被重复释放。  

- **[Auto Sync] Update loader.py, weight_utils.py (20260213)**  
  PR: <https://github.com/sgl-project/sglang/commit/008ea46af13e496240680932ad52c21b9e0589fb>  
  - `loader.py`（+/-2 行）和 `weight_utils.py`（+66 行，-45 行）同步最新模型加载逻辑。  

- **[bugfix] fix mamba slot leak when scheduling fails with radix cache**  
  PR: <https://github.com/sgl-project/sglang/commit/4c6afbeeaa8d80d59ace8b2707474a446e8a3596>  
  - `scheduler.py`（+21 行）修复 Radix 缓存调度失败时的槽位泄漏。  

- **refactor context parallel state**  
  PR: <https://github.com/sgl-project/sglang/commit/8b4c364960e6cccecd72edcdf47f54d06f3623c4>  
  - 大规模改动 `parallel_state.py`（+219 行，-16 行）以及多处调度、通信、注意力实现文件，整体提升并行上下文管理。  

- **[Kernel Slimming] Migrate GPTQ‑Marlin repack kernel to JIT**  
  PR: <https://github.com/sgl-project/sglang/commit/0012d6a4ebff8d87c5f9f99c295f85d3b69bf1e7>  
  - 新增 `gptq_marlin_repack.py`、`gptq_marlin_repack.cuh`、基准脚本及测试（共 +610 行），实现 JIT 编译的 GPTQ‑Marlin 重打包。  

- **[diffusion] chore: use batched P2P ops in VAE parallel decoding**  
  PR: <https://github.com/sgl-project/sglang/commit/37273408ebcde2d16890dc93d4e613825fc22f95>  
  - `wan_dist_utils.py`（+10 行，-7 行）改为批量 P2P 操作，提升 VAE 并行解码效率。  

- **[diffusion] fix typo**  
  PR: <https://github.com/sgl-project/sglang/commit/acc940d30217ea6ff67a278a2d894d136466aca2>  
  - `scale_residual_norm_scale_shift.py`（+1 行，-1 行）纠正拼写错误。  

- **[CI] Move `test_load_lora_from_tensor` test to H100**  
  PR: <https://github.com/sgl-project/sglang/commit/9a32f8ccb97697dd0d6a87ba323fcd2169e3aed9>  
  - `test_lora_load_from_tensor.py`（+1 行，-1 行）迁移至 H100 机器上执行。  

### 本周关键 Issue 汇总

- **[#18819] KeyError: 'ministral3'**  
  - 描述：在最新 SGLang 与 Transformers 0.5.1 组合下，加载 Ministral‑3 模型时报错 `KeyError: 'ministral3'`。  
  - 影响：模型无法启动，需兼容最新 Transformers。  

- **[#18818] FP8 KV cache scales from JSON not passed to `set_kv_buffer`**  
  - 描述：使用 `--quantization-param-path` 提供的 KV 缓存尺度在写入时始终为 `None`，导致 FP8 量化失效。  

- **[#18816] Feature: 自动检测文档链接失效**  
  - 描述：提议在 CI 中加入文档链接检查，以防止因路径重构导致的死链。  

- **[#18812] RotaryEmbedding fallback requires CUDA_HOME on HIP**  
  - 描述：在 ROCm 环境下，RotaryEmbedding 回退路径依赖 `CUDA_HOME`，导致 CI 失败。  

- **[#18799] Deepseek v3.2 prefill workers crash with PD disaggregation**  
  - 描述：在仅使用 TP=16（未启用 DP‑attention）时，Deepseek‑v3.2 的预填充工作进程在 DeepGEMM warmup 阶段崩溃。  

- **[#18798] Feature: 支持 Kimi K2.5 视频输入**  
  - 描述：请求为 Kimi K2.5 添加视频输入推理能力。  

- **[#18794] Feature: Qwen3‑Coder‑480B‑A35B B200/GB200 支持追踪**  
  - 描述：建立功能性、准确性以及不同并行配置（DEP、TP）的支持状态追踪表。  

- **[#18792] Bug: Qwen3 MoE 两批次重叠 warmup 失败**  
  - 描述：在 Qwen3‑MoE 使用两批次重叠时，warmup 前向阶段触发注意力层错误。  

> 本期简报聚焦于大幅度内核重构、分布式调度改进以及多模态/量化特性增强，整体代码改动约 **13 k 行**，其中新增文件超过 **30** 个，显著提升了 SGLang 在高性能推理、跨平台兼容以及测试可靠性方面的能力。
### vllm-project/vllm
## 提交

- **[d5fe3f7]** Enable mamba prefix cache “align” mode with async scheduling  
  PR: https://github.com/vllm-project/vllm/commit/d5fe3f702c2f4392515cdfde6fd0442271c74dda  
  关键文件: `vllm/v1/core/single_type_kv_cache_manager.py`（+13 行），`vllm/v1/worker/mamba_utils.py`（+6 / -1 行），`tests/v1/e2e/test_mamba_prefix_cache.py`（+58 / -19 行）。在异步调度下为 Mamba 前缀缓存加入对齐模式。*patch 已截断*。

- **[73391a1]** Move InputPreprocessor into Renderer (1/2)  
  PR: https://github.com/vllm-project/vllm/commit/73391a1baa459e78be1ade466517c7206ab7dd7c  
  关键文件: `vllm/inputs/preprocess.py`（-91 / +23 行），`vllm/entrypoints/llm.py`（+16 / -29 行），`vllm/engine/protocol.py`（+14 / -1 行）。将输入预处理逻辑迁入渲染层，统一入口。*patch 已截断*。

- **[b3c1422]** Guard sparse MLA backend imports for ROCm compatibility in tests  
  PR: https://github.com/vllm-project/vllm/commit/b3c14229b032a8bbf93d450a52c9a404ddaea429  
  关键文件: `tests/v1/attention/test_sparse_mla_backends.py`（+11 行）。在 ROCm 环境下避免不兼容的稀疏 MLA 导入。*patch 已截断*。

- **[2f18663]** Fix Qwen3.5 config loading  
  PR: https://github.com/vllm-project/vllm/commit/2f186635cbcb38fd85e718a5b7ff9ec698cbb4f8  
  关键文件: `vllm/transformers_utils/configs/qwen3_5.py`（+9 / -5 行），`vllm/transformers_utils/configs/qwen3_5_moe.py`（+9 / -5 行）。修复 Qwen3.5 配置解析错误。*patch 已截断*。

- **[342a7cd]** Update tests and examples for Prithvi/Terratorch models  
  PR: https://github.com/vllm-project/vllm/commit/342a7cda2d212205c4874f82a59559400bbec311  
  关键文件: `examples/pooling/plugin/prithvi_geospatial_mae_online.py`（+2 / -4 行），`tests/plugins_tests/test_io_processor_plugins.py`（+59 / -22 行）等。同步 Prithvi/Terratorch 示例与测试。*patch 已截断*。

- **[d1ea65d]** Add COLQwen3 code & Inference  
  PR: https://github.com/vllm-project/vllm/commit/d1ea65d0a1c606ae041b73fd45ccd33980ca08e7  
  关键文件: `vllm/model_executor/models/colqwen3.py`（+306 行），`examples/pooling/score/colqwen3_rerank_online.py`（+130 行），`tests/models/multimodal/pooling/test_colqwen3.py`（+156 行）。新增 COLQwen3 多模态模型及示例。*patch 已截断*。

- **[de42abb]** Heavy refactoring of Voxtral multimodal audio model tests  
  PR: https://github.com/vllm-project/vllm/commit/de42abb366032519bca073e057331ead6270e09f  
  关键文件: `tests/models/multimodal/generation/test_voxtral.py`（+138 / -43 行），`vllm/model_executor/models/voxtral.py`（+28 行）。重构 Voxtral 测试，提升可维护性。*patch 已截断*。

- **[60ca798]** Add explicit validation error for tool calls  
  PR: https://github.com/vllm-project/vllm/commit/60ca7981bce1bd6e2155df1a58bc9f916f7c4093  
  关键文件: `vllm/tokenizers/mistral.py`（+10 / -7 行）。在工具调用中加入明确的验证错误信息。*patch 已截断*。

- **[0ef5b91]** Use `__annotations__` instead of `get_type_hints()` for dynamic kwargs detection  
  PR: https://github.com/vllm-project/vllm/commit/0ef5b9147bb1f37c9a90ab2a3ee2a85cf9e84e30  
  关键文件: `vllm/transformers_utils/processor.py`（+11 / -1 行）。提升动态参数检测性能。*patch 已截断*。

- **[ed24265]** Make `get_modality_with_max_tokens` deterministic  
  PR: https://github.com/vllm-project/vllm/commit/ed242652d7f9cb4222e8840311b5229295b5d266  
  关键文件: `vllm/multimodal/encoder_budget.py`（+1 / -1 行）。确保多模态编码预算的确定性。*patch 已截断*。

- **[b37b679]** Support Selective CPU Weight Offloading  
  PR: https://github.com/vllm-project/vllm/commit/b37b679770aade27f33d20c93bf467c6a7fba65d  
  关键文件: `vllm/config/cache.py`（+11 行），`vllm/model_executor/models/utils.py`（+23 / -1 行）。实现对 CPU 权重的选择性卸载。*patch 已截断*。

- **[a0638d0]** Fix ROCm UVA CPU weight offloading broken by #32993  
  PR: https://github.com/vllm-project/vllm/commit/a0638d052db74ba28eada4768b9bbf98720b44a4  
  关键文件: `vllm/utils/torch_utils.py`（+1 / -1 行）。修复 ROCm 环境下的 CPU 权重卸载问题。*patch 已截断*。

- **[c027541]** Enable spec decoding in mamba cache align mode  
  PR: https://github.com/vllm-project/vllm/commit/c027541eaf05c6ca1e9a544804afe28caef671fc  
  关键文件: `vllm/model_executor/models/config.py`（-4 行），`tests/v1/e2e/test_mamba_prefix_cache.py`（+10 / -4 行）。在对齐模式下加入 spec 解码支持。*patch 已截断*。

- **[fd267bc]** Fix structured output in multi‑turn gpt‑oss  
  PR: https://github.com/vllm-project/vllm/commit/fd267bc7b7cd3d001ac5a893eacb9e56ff256822  
  关键文件: `vllm/reasoning/gptoss_reasoning_parser.py`（+9 行），`tests/reasoning/test_gptoss_reasoning_parser.py`（+18 / -1 行）。解决多轮 GPT‑OSS 结构化输出错误。*patch 已截断*。

- **[bfaa559]** Revert “Fix fused MoE IMA (sans chunking) by using int64 for strides”  
  PR: https://github.com/vllm-project/vllm/commit/bfaa5593050ec9bf60e2361b3b9dc575efeee83f  
  关键文件: `vllm/model_executor/layers/fused_moe/fused_moe.py`（+27 / -27 行）。恢复之前的 stride 类型实现。*patch 已截断*。

- **[87789c8]** `--enforce-eager` should turn off compile and cudagraphs only  
  PR: https://github.com/vllm-project/vllm/commit/87789c836422cf3b666ddf3eca9ede8e03f735ee  
  关键文件: `vllm/config/vllm.py`（+7 / -7 行）。细化 `--enforce-eager` 行为，仅禁用编译与 cudagraph。*patch 已截断*。

- **[bcd65c1]** Replace `c10::optional` with `std::optional` in topk kernel  
  PR: https://github.com/vllm-project/vllm/commit/bcd65c1f6a25ab76be325fbc0766eb074519a4fc  
  关键文件: `csrc/topk.cu`（+1 / -1 行）。提升 CUDA kernel 的可移植性。*patch 已截断*。

- **[59d5306]** Support CPU Offloading without PyTorch pinned memory (double allocation)  
  PR: https://github.com/vllm-project/vllm/commit/59d53066d8daed5c2e39e3bce38ac308bc80a9ae  
  关键文件: `vllm/model_executor/model_loader/utils.py`（+23 / -17 行），`tests/basic_correctness/test_cpu_offload.py`（+21 / -2 行）。实现不依赖 pinned memory 的 CPU 卸载路径。*patch 已截断*。

- **[4a9952e]** Add `quant_config` in ViT of Kimi‑K2.5  
  PR: https://github.com/vllm-project/vllm/commit/4a9952ec1b15453053f4ec443d2d81505d344075  
  关键文件: `vllm/model_executor/models/kimi_k25_vit.py`（+15 行）。为 ViT 添加量化配置。*patch 已截断*。

- **[1dae7b7]** Exclude `language_model_only` key from MM AOT compile hash but include in model one  
  PR: https://github.com/vllm-project/vllm/commit/1dae7b7843062f3468485653779d43ef96c7245c  
  关键文件: `vllm/config/model.py`（+6 行），`tests/config/test_multimodal_config.py`（+18 行）。细化多模态 AOT 编译哈希策略。*patch 已截断*。

- **[5885e33]** Port Qwen3.5 Configs  
  PR: https://github.com/vllm-project/vllm/commit/5885e330efea5212733375f3573990de791d5042  
  关键文件: `vllm/transformers_utils/configs/qwen3_5.py`（+189 行），`vllm/transformers_utils/configs/qwen3_5_moe.py`（+201 行）。新增 Qwen3.5 系列配置。*patch 已截断*。

- **[071d863]** Extend ColBERT support to non‑standard BERT backbones  
  PR: https://github.com/vllm-project/vllm/commit/071d863e208b40fa1bb986ad230e322b2bbbbcf5  
  关键文件: `vllm/model_executor/models/colbert.py`（+335 / -68 行），`tests/models/language/pooling/test_colbert.py`（+242 / -73 行）。让 ColBERT 能兼容自定义 BERT。*patch 已截断*。

- **[0916e79]** Use CPU tensors to build GDN metadata  
  PR: https://github.com/vllm-project/vllm/commit/0916e7960bddf565c33153d2cf753e799de105b7  
  关键文件: `vllm/v1/attention/backends/gdn_attn.py`（+10 / -7 行）。改为 CPU 张量生成 GDN 元数据，提高兼容性。*patch 已截断*。

- **[3d2a026]** Pipeline Parallel Async send/recv, 2.9% E2E throughput improvement  
  PR: https://github.com/vllm-project/vllm/commit/3d2a026fd0317204752d7933408aff19aaa80cfd  
  关键文件: `vllm/distributed/parallel_state.py`（+137 / -76 行），`vllm/v1/worker/gpu_worker.py`（+54 / -5 行）。实现异步通信，提升管线并行吞吐。*patch 已截断*。

- **[dddbff4]** Move pause and resume functions into engine  
  PR: https://github.com/vllm-project/vllm/commit/dddbff46242a9292085e2ae3309dc559f242cad6  
  关键文件: `vllm/v1/engine/core.py`（+125 / -50 行），`vllm/v1/engine/async_llm.py`（-49 / +6 行），新增示例 `examples/online_serving/data_parallel_pause_resume.py`（+135 行）。将暂停/恢复逻辑迁入引擎层。*patch 已截断*。

- **[47e9b63]** Clean up redundant code in KV connectors  
  PR: https://github.com/vllm-project/vllm/commit/47e9b63e1afeb074b0fa584e0169e27d517b4e7b  
  关键文件: `vllm/distributed/kv_transfer/kv_connector/v1/example_connector.py`（-8 行），`vllm/distributed/kv_transfer/kv_connector/v1/offloading_connector.py`（+1 / -1 行）。精简 KV 连接器实现。*patch 已截断*。

- **[934acdd]** fused_moe: add int4_w4a16 benchmark support and tuning config  
  PR: https://github.com/vllm-project/vllm/commit/934acddef9fa4eb1b6cc897d2e39db77385539c6  
  关键文件: `benchmarks/kernels/benchmark_moe.py`（+122 / -8 行），新增配置 `fused_moe/configs/E=128,N=768,device_name=Radeon_8060S_Graphics,dtype=int4_w4a16.json`（+63 行）。支持 int4_w4a16 基准。*patch 已截断*。

- **[742d214]** Fix import path in moe test utils.py  
  PR: https://github.com/vllm-project/vllm/commit/742d214d6eeb1b0c92aabae36614be6a485fb94d  
  关键文件: `tests/kernels/moe/utils.py`（+5 / -5 行）。纠正测试工具的导入路径。*patch 已截断*。

- **[4137c5d]** Fix MambaManager.cache_blocks() crash on null blocks in align mode  
  PR: https://github.com/vllm-project/vllm/commit/4137c5dfa7c0de6c0ff74ad3774224b6b3280349  
  关键文件: `vllm/v1/core/single_type_kv_cache_manager.py`（+2 行），`tests/v1/core/test_prefix_caching.py`（+46 行）。防止空块导致的崩溃
### NVIDIA/cutile-python
**提交**  
- **SHA**: `394d302`  
- **链接**: https://github.com/NVIDIA/cutile-python/commit/394d302ae6b31f988cb91d80b466c6a695405a43  
- **作者**: Jay Gu  
- **日期**: 2026‑02‑11 21:53:31 UTC  
- **信息**: Create temp dir if not exist  

**文件变更**  

| 模块/文件 | 状态 | 增删行数 | 说明 |
|---|---|---|---|
| `src/cuda/tile/_compile.py` | modified | +2 / -2 | 代码中加入了创建临时目录的逻辑。<br>**Diff**：未提供（`patch_truncated` 为 true）。 |
| `src/cuda/tile/_context.py` | modified | +2 / 0 | 同样添加了临时目录检查。<br>**Diff**：未提供（`patch_truncated` 为 true）。 |

**总体概述**  
本次提交在 CUDA Tile 模块的编译与上下文管理代码中，加入了“若临时目录不存在则自动创建”的容错处理，提升了运行时的鲁棒性。由于补丁内容被截断，具体实现细节未展示。

## 总结
🚀 **趋势**：AI 基础设施正向 **DSL‑驱动的高效编译、跨硬件统一加速、以及多模态大模型的全链路优化** 迈进。**关注点**：随着 CUDA 13.1、SM120 与 ROCm 的快速迭代，保持代码库的跨平台兼容性与 AOT/JIT 缓存策略将是下一步的关键。
